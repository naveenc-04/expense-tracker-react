# Expense Tracker Dashboard

A simple React.js web application that helps users track their daily expenses by category.  
The app allows users to add, delete, and view expenses, with persistent storage using browser localStorage.

## 🚀 Features
- Add and delete expenses
- Categorize expenses (Food, Travel, Shopping, Other)
- Real-time total expense calculation
- Data persistence using localStorage
- Clean and responsive UI

## 🛠 Tech Stack
- React.js
- JavaScript (ES6)
- CSS
- Browser LocalStorage

## 📂 Project Structure
- Component-based architecture
- State management using React Hooks (`useState`, `useEffect`)

## ▶️ How to Run Locally
1. Clone the repository  
   `git clone https://github.com/your-username/expense-tracker-react.git`
2. Navigate to project folder  
   `cd expense-tracker-react`
3. Install dependencies  
   `npm install`
4. Start the app  
   `npm start`
5. Open `http://localhost:3000` in your browser

## 📌 Future Improvements
- Expense charts and analytics
- Filter expenses by date or category
- Authentication and backend integration
